import 'package:flutter/material.dart';

class AtividadeView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Atividades')),
      body: Center(child: Text('Lista de Atividades')),
    );
  }
}